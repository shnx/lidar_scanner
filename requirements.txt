# SLAM Launcher — Python dependencies
# Install with: pip3 install -r requirements.txt
#
# ROS Melodic provides: rospy, sensor_msgs, nav_msgs, geometry_msgs
# Those are NOT listed here since they are managed by apt / catkin.

# ── GUI ──────────────────────────────────────────────────────────────────────
PyQt5>=5.12.3,<5.16
PyQt5-sip>=12.7

# ── 3-D Visualization ────────────────────────────────────────────────────────
open3d==0.16.0          # last release tested on Ubuntu 18.04 / Python 3.6+

# ── Data & Math ──────────────────────────────────────────────────────────────
numpy>=1.17.0
scipy>=1.3.0

# ── Config parsing ────────────────────────────────────────────────────────────
PyYAML>=5.1

# ── Optional: richer terminal output during dev ──────────────────────────────
# colorlog>=4.0
